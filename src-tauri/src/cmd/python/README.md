# Python Mode Dependencies

This module handles Python code execution.

## System Requirements

- `python3`: Python interpreter.
- `pip`: Python package manager (optional, for libraries).

## Installation (macOS)

Usually pre-installed or via:

```bash
brew install python
```
